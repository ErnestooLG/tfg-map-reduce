# Ruta donde estan las guias PDF.
# Si los PDFs estan en otra carpeta, cambia solo esta ruta.
ruta_pdfs_por_asignatura: "data/guias"

# Modelo de Gemini usado para la fase MAP y el reduce opcional con LLM.
modelo: "gemini-2.5-flash-lite"
